import Canvas from '../src/routes/art/common/canvas'

describe('Test canvas element', () => {
  test('Canvas is as expected', () => {
    // const context = shallow(<Canvas />)
    expect(Canvas).toBeTruthy()
  })
})
