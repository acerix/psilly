import GridOverlay from "../src/routes/art/common/grid-overlay"

describe('Test grid', () => {

  test('GridOverlay centre is centred', () => {
    expect(GridOverlay).toBeTruthy()
    // const go = new GridOverlay({})
    // expect(go.translate).toStrictEqual([0, 0])
  })

})
